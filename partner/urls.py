from django.urls import path
#from .views import ZoneManagerAutocomplete, ASMAutocomplete, DistrictAutocomplete, OfficeAutocomplete

urlpatterns = [
    # path("zone-manager-autocomplete/", ZoneManagerAutocomplete.as_view(), name="zone-manager-autocomplete"),
    # path("asm-autocomplete/", ASMAutocomplete.as_view(), name="asm-autocomplete"),
    # path("district-autocomplete/", DistrictAutocomplete.as_view(), name="district-autocomplete"),
    # path("office-autocomplete/", OfficeAutocomplete.as_view(), name="office-autocomplete"),
]
